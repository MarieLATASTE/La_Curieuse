<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {# Run `composer require symfony/webpack-encore-bundle`
           and uncomment the following Encore helpers to start using Symfony UX #}
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
        <link rel="stylesheet" href="https://bootswatch.com/4/sketchy/bootstrap.min.css">
        <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.7.0/css/all.css">

        {% block stylesheets %}{#{{ encore_entry_link_tags('app') }}#}{% endblock %}
        {% block javascripts %}{#{{ encore_entry_script_tags('app') }}#}{% endblock %}
    </head>

    <body>

               <h1 class="mx-auto" style="width:300px;">Curiosi...quoi?</h1>

                <nav class="navbar navbar-expand-md navbar-dark bg-primary sticky-top">
        <a class="navbar-brand" href="{{ path('home') }}">Curiosiquoi</a>

        <div class="collapse navbar-collapse" id="navbarColor01">
            <ul class="navbar-nav mr-auto">

            <li class="nav-item">
                <a class="nav-link" href="{{ path('category_show', {'id':1}) }}">Deco</a>
            </li>

            <li class="nav-item">
                <a class="nav-link" href="{{ path('category_show', {'id':2}) }}">Stuff</a>
            </li>

            <li class="nav-item">
                <a class="nav-link" href="{{ path('category_show', {'id':3}) }}">Quoi</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('user') }}">Staff</a>
            </li>

            <li class="nav-item">
              <a class="nav-link" href="{{ path('security_registration' )}}">Join here</a>
            </li>

            {% if not app.user %}

            <li class="nav-item">
              <a class="nav-link" href="{{ path('security_login') }}"><i class='fas fa-dragon' style='font-size:24px;color:red'></i></a>
            </li> 

            {% else %}

            <li class="nav-item">
              <a class="nav-link" href="{{ path('security_logout' )}}"><i class='fas fa-dragon' style='font-size:24px;color:green'></i></a>
            </li>

          {% endif %}

          <li class="nav-item">
                <a class="nav-link" href="{{ path('home') }}">Contact</a>
            </li>

            <li class="nav-item">
                <a class="nav-link" href="{{ path('cart_panier') }}"><i class='fas fa-cart-arrow-down' style='font-size:24px;color:white'></i></a>
            </li>

            </ul>
            </form>
        </div>
        </nav>
      

<div class="fixed-bottom">
<table class="table table-responsive-md">

  <tbody>
    <tr class="table-dark">
      <th scope="row"></th>
      <td><i class='fab fa-github' style='font-size:24px'></i></td>
      <td>Curiosiquoi &copy; 2021</td>
      <td><button onclick="topFunction()" type="button" id="myBtn" title="Go to top" class="btn btn-outline-light">Top</button></td>
    </tr>
  </tbody>
</table>
</div>

<script>
//Get the button
var mybutton = document.getElementById("myBtn");

// When the user scrolls down 20px from the top of the document, show the button
window.onscroll = function() {scrollFunction()};

function scrollFunction() {
  if (document.body.scrollTop > 20 || document.documentElement.scrollTop > 20) {
    mybutton.style.display = "block";
  } else {
    mybutton.style.display = "none";
  }
}

// When the user clicks on the button, scroll to the top of the document
function topFunction() {
  document.body.scrollTop = 0;
  document.documentElement.scrollTop = 0;
}
</script>

        <div class="container">
            {% block body %}
            {% endblock %}
        </div>
    </body>
</html>
